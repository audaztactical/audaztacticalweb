import TimerModePlaceholder from '../TimerModePlaceholder'

/** @param {{ modeTitle: string, opsCode: string }} props */
export default function StructuralFrameMode({ modeTitle, opsCode }) {
  return <TimerModePlaceholder modeTitle={modeTitle} opsCode={opsCode} />
}
