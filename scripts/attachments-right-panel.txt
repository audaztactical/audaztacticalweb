        <TacticalPanel className="flex min-h-0 flex-col overflow-hidden border-white/10 bg-black/40 p-0">
          <p className="shrink-0 border-b border-white/10 bg-[#080808] px-3 py-2 font-mono-technical text-[8px] font-bold uppercase tracking-[0.24em] text-[#00FF41]/80">
            EYLEM · DENETİM_HUB
          </p>
          {selected ? (
            <div className="flex min-h-0 flex-1 flex-col">
              {!mounted ? (
                <div className="shrink-0 space-y-2 border-b border-white/10 p-3">
                  <p className="font-mono-technical text-[8px] font-bold uppercase text-slate-500">MONTAJ / SÖKME</p>
                  <label className="block">
                    <span className="font-mono-technical text-[7px] uppercase text-slate-500">HEDEF_SİLAH</span>
                    <select
                      className={`${selectClass} mt-1`}
                      value={mountWeaponId}
                      onChange={(e) => setMountWeaponId(e.target.value)}
                    >
                      <option value="">— SİLAH SEÇ —</option>
                      {weapons.map((w) => (
                        <option key={String(w.id)} value={String(w.id)}>
                          [{weaponStokKodu(String(w.id))}] {weaponDisplayName(w)}
                        </option>
                      ))}
                    </select>
                  </label>
                  <button
                    type="button"
                    disabled={busy || !mountWeaponId}
                    onClick={mountAccessory}
                    className="w-full rounded border border-[#00FF41]/50 bg-[#00FF41]/12 py-2 font-mono-technical text-[9px] font-bold uppercase text-[#00FF41] hover:bg-[#00FF41]/20 disabled:opacity-40"
                  >
                    [ ⚡ MONTAJ ET / MOUNT ]
                  </button>
                  <label className="block">
                    <span className="font-mono-technical text-[7px] uppercase text-slate-500">İŞLEM_TARİHİ (KAYIT)</span>
                    <input
                      type="date"
                      className={`${dateInputClass} mt-1`}
                      value={logDate}
                      max={todayIsoDate()}
                      onChange={(e) => setLogDate(e.target.value)}
                    />
                  </label>
                </div>
              ) : null}

              <div className="flex shrink-0 gap-1 border-b border-white/10 bg-[#080808] p-1">
                <button
                  type="button"
                  onClick={() => setRightTab('deploy')}
                  className={`flex-1 rounded px-2 py-1.5 font-mono-technical text-[7px] font-bold uppercase tracking-wide ${
                    rightTab === 'deploy'
                      ? 'border border-[#00FF41]/50 bg-[#00FF41]/15 text-[#00FF41]'
                      : 'text-slate-500 hover:text-slate-300'
                  }`}
                >
                  [ 📑 MONTAJ KAYITLARI ]
                </button>
                <button
                  type="button"
                  onClick={() => setRightTab('maint')}
                  className={`flex-1 rounded px-2 py-1.5 font-mono-technical text-[7px] font-bold uppercase tracking-wide ${
                    rightTab === 'maint'
                      ? 'border border-[#ffb400]/50 bg-[#ffb400]/15 text-[#ffb400]'
                      : 'text-slate-500 hover:text-slate-300'
                  }`}
                >
                  [ 🛠️ TEKNİK BAKIM ]
                </button>
              </div>

              <div className={`min-h-0 flex-1 p-3 ${panelScroll}`}>
                {rightTab === 'deploy' ? (
                  <section>
                    <div className="mb-2 flex flex-wrap items-end justify-between gap-2">
                      <p className="font-mono-technical text-[8px] font-bold uppercase tracking-wider text-[#00FF41]/90">
                        AKSESUAR MONTAJ / SÖKME KAYIT DEFTERİ
                      </p>
                      <span className="font-mono-technical text-[7px] tabular-nums text-slate-500">
                        {filteredDeploymentHistory.length}/{deploymentHistory.length}
                      </span>
                    </div>
                    <div className="mb-2 grid gap-1.5 rounded border border-[#00FF41]/20 bg-black/30 p-2">
                      <p className="font-mono-technical text-[7px] font-bold uppercase text-slate-600">FİLTRE</p>
                      <div className="grid gap-1.5 sm:grid-cols-3">
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">İŞLEM TÜRÜ</span>
                          <select
                            className={filterSelectClass}
                            value={deployActionFilter}
                            onChange={(e) => setDeployActionFilter(e.target.value)}
                          >
                            {DEPLOY_ACTION_FILTERS.map((f) => (
                              <option key={f.value} value={f.value}>
                                {f.label}
                              </option>
                            ))}
                          </select>
                        </label>
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">TARİH BAŞ</span>
                          <input
                            type="date"
                            className={filterDateClass}
                            value={deployDateFrom}
                            max={deployDateTo || todayIsoDate()}
                            onChange={(e) => setDeployDateFrom(e.target.value)}
                          />
                        </label>
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">TARİH BİT</span>
                          <input
                            type="date"
                            className={filterDateClass}
                            value={deployDateTo}
                            min={deployDateFrom || undefined}
                            max={todayIsoDate()}
                            onChange={(e) => setDeployDateTo(e.target.value)}
                          />
                        </label>
                      </div>
                      {(deployActionFilter !== 'ALL' || deployDateFrom || deployDateTo) && (
                        <button
                          type="button"
                          onClick={() => {
                            setDeployActionFilter('ALL')
                            setDeployDateFrom('')
                            setDeployDateTo('')
                          }}
                          className="font-mono-technical text-[7px] uppercase text-[#00FF41]/80 hover:text-[#00FF41]"
                        >
                          FİLTREYİ SIFIRLA
                        </button>
                      )}
                    </div>
                    <ul className={`max-h-[400px] space-y-1.5 ${panelScroll}`}>
                      {deploymentHistory.length === 0 ? (
                        <li className="font-mono-technical text-[9px] uppercase text-slate-600">KAYIT_YOK</li>
                      ) : filteredDeploymentHistory.length === 0 ? (
                        <li className="font-mono-technical text-[9px] uppercase text-slate-600">FİLTRE_SONUCU_YOK</li>
                      ) : (
                        filteredDeploymentHistory.map((log, i) => (
                          <li
                            key={`${log.date}-${log.action_type}-${i}`}
                            className="rounded border border-white/8 bg-black/40 px-2 py-1.5 font-mono-technical text-[8px] uppercase"
                          >
                            <span className="text-slate-500">{log.date}</span>
                            <span className={`ml-2 ${log.action_type === 'MONTAJ' ? 'text-[#00FF41]' : 'text-amber-400'}`}>
                              {log.action_type}
                            </span>
                            <p className="mt-0.5 text-slate-300">{log.target_weapon}</p>
                          </li>
                        ))
                      )}
                    </ul>
                  </section>
                ) : (
                  <section className="space-y-3">
                    <p className="font-mono-technical text-[8px] font-bold uppercase tracking-wider text-[#ffb400]/90">
                      TEKNİK BAKIM VE ONARIM GÜNLÜĞÜ
                    </p>
                    <form
                      onSubmit={submitTechnicalMaintenance}
                      className="space-y-2 rounded border border-[#00FF41]/20 bg-black/50 p-2"
                    >
                      <p className="font-mono-technical text-[7px] font-bold uppercase text-[#ffb400]/80">+ YENİ TEKNİK KAYIT</p>
                      <label className="block">
                        <span className="font-mono-technical text-[7px] font-bold uppercase tracking-wider text-slate-500">BAKIM TÜRÜ:</span>
                        <select
                          className={`${selectClass} mt-1`}
                          value={techMaintType}
                          onChange={(e) => setTechMaintType(e.target.value)}
                          disabled={techMaintSaving || busy}
                        >
                          {ACCESSORY_MAINTENANCE_TYPES.map((t) => (
                            <option key={t} value={t}>{t}</option>
                          ))}
                        </select>
                      </label>
                      <label className="block">
                        <span className="font-mono-technical text-[7px] font-bold uppercase tracking-wider text-slate-500">TARİH:</span>
                        <input
                          type="date"
                          className={`${dateInputClass} mt-1`}
                          value={techMaintDate}
                          max={todayIsoDate()}
                          onChange={(e) => setTechMaintDate(e.target.value)}
                          disabled={techMaintSaving || busy}
                          required
                        />
                      </label>
                      <label className="block">
                        <span className="font-mono-technical text-[7px] font-bold uppercase tracking-wider text-slate-500">TEKNİK AÇIKLAMA:</span>
                        <textarea
                          className={textareaClass}
                          placeholder="Teknik müdahale veya onarım detaylarını buraya işleyin..."
                          value={techMaintNote}
                          onChange={(e) => setTechMaintNote(e.target.value)}
                          disabled={techMaintSaving || busy}
                          rows={3}
                        />
                      </label>
                      <button
                        type="submit"
                        disabled={techMaintSaving || busy}
                        className="w-full rounded border border-[#00FF41]/45 bg-[#00FF41]/12 py-1.5 font-mono-technical text-[8px] font-bold uppercase text-[#00FF41] hover:bg-[#00FF41]/16 disabled:opacity-50"
                      >
                        {techMaintSaving ? '…' : 'KAYDET'}
                      </button>
                    </form>
                    <div className="mb-2 grid gap-1.5 rounded border border-white/8 bg-black/30 p-2">
                      <p className="font-mono-technical text-[7px] font-bold uppercase text-slate-600">GEÇMİŞ · FİLTRE</p>
                      <div className="grid gap-1.5 sm:grid-cols-3">
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">BAKIM TÜRÜ</span>
                          <select className={filterSelectClass} value={techTypeFilter} onChange={(e) => setTechTypeFilter(e.target.value)}>
                            <option value="ALL">TÜM TÜRLER</option>
                            {ACCESSORY_MAINTENANCE_TYPES.map((t) => (
                              <option key={t} value={t}>{t}</option>
                            ))}
                          </select>
                        </label>
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">TARİH BAŞ</span>
                          <input type="date" className={filterDateClass} value={techDateFrom} max={techDateTo || todayIsoDate()} onChange={(e) => setTechDateFrom(e.target.value)} />
                        </label>
                        <label className="block">
                          <span className="font-mono-technical text-[7px] uppercase text-slate-500">TARİH BİT</span>
                          <input type="date" className={filterDateClass} value={techDateTo} min={techDateFrom || undefined} max={todayIsoDate()} onChange={(e) => setTechDateTo(e.target.value)} />
                        </label>
                      </div>
                      {(techTypeFilter !== 'ALL' || techDateFrom || techDateTo) && (
                        <button type="button" onClick={() => { setTechTypeFilter('ALL'); setTechDateFrom(''); setTechDateTo('') }} className="font-mono-technical text-[7px] uppercase text-[#ffb400]/80 hover:text-[#ffb400]">
                          FİLTREYİ SIFIRLA
                        </button>
                      )}
                    </div>
                    <ul className="space-y-0">
                      {technicalMaintenanceLogs.length === 0 ? (
                        <li className="font-mono-technical text-[9px] uppercase text-slate-600">TEKNİK_KAYIT_YOK</li>
                      ) : filteredTechnicalLogs.length === 0 ? (
                        <li className="font-mono-technical text-[9px] uppercase text-slate-600">FİLTRE_SONUCU_YOK</li>
                      ) : (
                        filteredTechnicalLogs.map((log, i) => (
                          <li key={`${log.date}-${log.maintenanceType}-${i}`}>
                            {i > 0 ? <div className="my-2 font-mono-technical text-[8px] text-slate-600" aria-hidden>-------------------------</div> : null}
                            <p className="font-mono-technical text-[9px] tabular-nums text-slate-500">{log.date}</p>
                            <span className="mt-0.5 inline-block rounded border border-[#ffb400]/40 bg-[#ffb400]/10 px-1.5 py-0.5 font-mono-technical text-[7px] font-bold uppercase text-[#ffb400]">{log.maintenanceType}</span>
                            <p className="mt-1 font-mono-technical text-[9px] normal-case leading-snug text-slate-300">{log.note || '—'}</p>
                          </li>
                        ))
                      )}
                    </ul>
                  </section>
                )}
              </div>
            </div>
          ) : (
            <p className="flex flex-1 items-center justify-center p-6 font-mono-technical text-[9px] uppercase text-slate-600">AKSESUAR_SEÇİN</p>
          )}
        </TacticalPanel>
